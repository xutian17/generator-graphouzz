'use strict';

module.exports = Object.freeze({
    GENERATE_NODE: {
        name: 'generateNode',
        message: 'Do you want to create a new graph node? (Default: yes) ? : ',
        type: 'confirm',
        default: 'y'
    }
});
